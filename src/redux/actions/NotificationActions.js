import { createAction } from '@reduxjs/toolkit';

export const ADD_NOTIFICATION_ACTION = createAction('ADD_NOTIFICATION');
export const CLOSE_NOTIFICATION_ACTION = createAction('CLOSE_NOTIFICATION');
